# ========================================
# AY Trading Strategy 环境变量配置示例
# ========================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填入你的真实配置信息
# 3. .env 文件不会被提交到 Git 仓库
# ========================================

# ========== 数据源配置 ==========

# Tushare API Token (必填，用于专业数据源)
# 获取方式：https://tushare.pro/register
TUSHARE_TOKEN=your_tushare_token_here

# AKShare 配置（可选）
# AKSHARE_TIMEOUT=30

# ========== 数据存储配置 ==========

# 股票数据存储目录（可选，默认为项目下的 data/stock_data）
# STOCK_DATA_DIR=/path/to/your/stock_data

# 缓存目录（可选，默认为 data_cache）
# CACHE_DIR=data_cache

# ========== 通知配置 ==========

# 企业微信 Webhook URL（可选，用于消息推送）
# WECOM_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key

# 钉钉 Webhook URL（可选）
# DINGTALK_WEBHOOK_URL=https://oapi.dingtalk.com/robot/send?access_token=your_token

# ========== 应用配置 ==========

# 日志级别（可选，默认为 INFO）
# LOG_LEVEL=INFO

# 日志保留天数（可选，默认为 30）
# LOG_RETENTION_DAYS=30

# 缓存过期时间（分钟，可选，默认为 30）
# CACHE_TTL_MINUTES=30

# 每分钟最大请求数（可选，默认为 15）
# MAX_REQUESTS_PER_MINUTE=15

# ========== 数据库配置（未来扩展） ==========

# DATABASE_URL=sqlite:///data/stocks.db
# REDIS_URL=redis://localhost:6379/0

# ========== 开发环境配置 ==========

# 开发模式（可选，默认为 False）
# DEBUG=False

# Streamlit 端口（可选，默认为 8501）
# STREAMLIT_PORT=8501
